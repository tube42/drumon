package com.badlogic.gdx.backends.lwjgl;

// this is here so we can compile the desktop files if it has not been excluded
public class LwjglApplication
{
    public LwjglApplication(Object o, String s, int x, int y)
    {
    }
}
